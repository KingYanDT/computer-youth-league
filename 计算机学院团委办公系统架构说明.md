# 计算机学院团委工作系统 - 架构说明

## 目录
1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [目录结构](#目录结构)
4. [数据库设计](#数据库设计)
5. [后端架构](#后端架构)
6. [前端架构](#前端架构)
7. [用户权限与角色](#用户权限与角色)
8. [功能模块](#功能模块)
9. [安全机制](#安全机制)
10. [部署说明](#部署说明)
11. [系统架构图](#系统架构图)
12. [更新日志](#更新日志)

---

## 项目概述

**项目名称**：计算机学院团委办公系统  
**版本**：2.4.0  
**描述**：面向高校计算机学院团委的办公管理系统，用于任务发布、文件提交、审核、汇总等全流程管理。支持本地开发与云端服务器部署，已通过全面安全加固。前端采用单文件架构，UI精美，交互流畅。

---

## 技术栈

### 后端技术
- **框架**：Express.js 4.18.2
- **数据库**：MySQL 8.0+
- **ORM**：mysql2 (原生SQL + Promise API)
- **文件上传**：Multer 1.4.5-lts.1（类型白名单 + 大小限制）
- **认证**：JWT (jsonwebtoken 9.0.2) + bcryptjs (密码加密) + httpOnly Cookie
- **安全**：helmet (安全响应头), express-rate-limit (速率限制), cookie-parser
- **其他**：cors (跨域域名限制), dotenv (环境变量)

### 前端技术
- **框架**：原生 JavaScript (无第三方UI框架)
- **样式**：CSS3 (CSS变量 + 渐变 + 动画)
- **架构模式**：单文件SPA (Single Page Application)，内嵌CSS + HTML + JS
- **数据交互**：Fetch API + Cookie认证，后端API驱动

### 运维工具
- **进程管理**：PM2 5.3.0
- **反向代理**：Nginx 1.18+ (HTTPS + HSTS)
- **开发工具**：nodemon 3.0.0

---

## 目录结构

```
计算机学院团委工作系统/
├── backend/                      # 后端代码目录
│   ├── config/
│   │   └── db.js                 # 数据库连接配置
│   ├── middleware/
│   │   └── auth.js               # JWT认证中间件（Cookie + token_version）
│   ├── routes/
│   │   ├── auth.js               # 认证路由（含速率限制）
│   │   ├── users.js              # 用户管理路由
│   │   ├── departments.js        # 部门管理路由（支持minister_id + member_id）
│   │   ├── tasks.js              # 任务管理路由
│   │   ├── files.js              # 文件提交路由（含认证下载 + 重新提交）
│   │   ├── summary.js            # 汇总文件路由
│   │   └── notifications.js      # 通知路由
│   ├── utils/
│   │   └── auditLog.js           # 审计日志工具
│   └── server.js                 # 后端入口文件（helmet + CORS + 文件限制）
├── frontend/                     # 前端代码目录
│   └── index.html                # 单文件SPA（CSS + HTML + JS 全部内嵌）
├── database/
│   └── init.js                   # 数据库初始化脚本
├── deploy/                       # 部署配置目录
│   ├── nginx.conf                # Nginx配置（HTTPS + HSTS + uploads deny）
│   └── deploy.sh                 # 一键部署脚本
├── uploads/                      # 文件上传目录（自动创建，Nginx deny all）
├── logs/                         # PM2日志目录（自动创建）
├── .env.example                  # 环境变量模板
├── .env                          # 开发环境配置（不提交到Git）
├── .gitignore                    # Git忽略规则
├── ecosystem.config.js           # PM2进程管理配置
├── package.json                  # 项目依赖配置
└── 计算机学院团委办公系统架构说明.md  # 本文档
```

---

## 数据库设计

### 数据库名
`youth_league`

### 表关系图

```
departments ──1:N── users
     │               │
     │               ├──1:N── notifications
     │               │
     │               └──1:N── audit_logs
     │
     └──1:N── tasks
                │
                ├──1:N── file_submissions
                │
                └──1:N── summary_files
```

### 表结构

#### 1. departments (部门表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(50) | 部门名称 |
| color | VARCHAR(20) | 部门标签颜色 |
| minister_id | VARCHAR(36) | 部长用户ID |
| member_id | VARCHAR(36) | 成员用户ID |
| created_at | DATETIME | 创建时间 |

#### 2. users (用户表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(50) | 真实姓名 |
| username | VARCHAR(50) | 登录用户名（唯一） |
| password | VARCHAR(255) | 密码（bcrypt加密） |
| role | ENUM | 角色：secretary/viceSecretary/minister/viceMinister/member/branchSecretary |
| department_id | VARCHAR(36) | 所属部门ID（外键→departments.id） |
| token_version | INT | Token版本号，修改密码时递增使旧Token失效 |
| created_at | DATETIME | 创建时间 |

#### 3. tasks (任务表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| title | VARCHAR(100) | 任务标题 |
| description | TEXT | 任务描述 |
| category | ENUM | 分类：组织生活/学习教育/主题活动/日常工作/其他 |
| year | INT | 年份 |
| month | INT | 月份 |
| day | INT | 日期（可选） |
| frequency | ENUM | 频率：每周/每月/每季度/每学期/每年/不定期 |
| time_slot | VARCHAR(100) | 时间段描述 |
| department_id | VARCHAR(36) | 负责部门ID（外键→departments.id） |
| assigned_to | ENUM | 指派对象：all/branchSecretaries |
| created_by | VARCHAR(36) | 创建者用户ID（外键→users.id） |
| is_regular | BOOLEAN | 是否常规任务 |
| status | ENUM | 状态：active/completed |
| created_at | DATETIME | 创建时间 |

#### 4. file_submissions (文件提交表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| task_id | VARCHAR(36) | 关联任务ID（外键→tasks.id，级联删除） |
| file_name | VARCHAR(255) | 原始文件名 |
| file_path | VARCHAR(500) | 存储路径 |
| file_size | BIGINT | 文件大小（字节） |
| submitted_by | VARCHAR(36) | 提交者用户ID（外键→users.id） |
| submitted_by_name | VARCHAR(50) | 提交者姓名 |
| department_id | VARCHAR(36) | 提交者部门ID（外键→departments.id） |
| submitted_at | DATETIME | 提交时间 |
| status | ENUM | 审核状态：pending/approved/returned |
| returned_by | VARCHAR(36) | 退回者用户ID |
| returned_at | DATETIME | 退回时间 |
| return_reason | TEXT | 退回原因 |

#### 5. summary_files (汇总文件表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| task_id | VARCHAR(36) | 关联任务ID（外键→tasks.id，级联删除） |
| file_name | VARCHAR(255) | 文件名 |
| file_path | VARCHAR(500) | 存储路径 |
| file_size | BIGINT | 文件大小 |
| uploaded_by | VARCHAR(36) | 上传者用户ID（外键→users.id） |
| department_id | VARCHAR(36) | 部门ID（外键→departments.id） |
| uploaded_at | DATETIME | 上传时间 |

#### 6. notifications (通知表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| type | ENUM | 类型：task/file/system |
| title | VARCHAR(100) | 通知标题 |
| message | TEXT | 通知内容 |
| target_user | VARCHAR(36) | 目标用户ID（外键→users.id，级联删除） |
| is_read | BOOLEAN | 是否已读 |
| created_at | DATETIME | 创建时间 |

#### 7. audit_logs (审计日志表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| user_id | VARCHAR(36) | 操作者用户ID（索引） |
| action | VARCHAR(50) | 操作类型（索引） |
| target_type | VARCHAR(50) | 目标类型 |
| target_id | VARCHAR(36) | 目标ID |
| details | TEXT | 操作详情（JSON） |
| ip_address | VARCHAR(45) | 操作者IP地址 |
| created_at | DATETIME | 操作时间（索引） |

---

## 后端架构

### 入口文件 (backend/server.js)
- **安全中间件**：helmet（安全响应头）、cors（域名限制）、cookie-parser
- **静态资源服务**：仅前端文件（`/uploads` 已移除静态服务，走认证下载接口）
- **文件上传**：Multer（类型白名单 + 大小限制 + fileFilter + 错误处理）
- **API路由挂载**：`/api/*`
- **SPA路由回退**：所有非API请求返回 `index.html`

### 认证中间件 (backend/middleware/auth.js)
- `authenticateToken`：从 httpOnly Cookie 读取 JWT，校验 `token_version` 一致性
- `requireRole(...roles)`：角色权限校验
- JWT密钥：**必须**通过环境变量 `JWT_SECRET` 配置，未设置时服务器拒绝启动
- Token有效期：24小时
- 修改密码时递增 `token_version`，所有旧 Token 立即失效

### 审计日志 (backend/utils/auditLog.js)
- 记录关键操作（登录/退出/修改密码/文件提交/审批/打回/上传汇总/删除/重新提交）
- 写入失败不影响主流程
- 字段：user_id、action、target_type、target_id、details、ip_address

### API路由总览

| 路由前缀 | 功能 | 文件 |
|----------|------|------|
| `/api/auth` | 登录、退出、获取用户信息、修改密码（含速率限制） | auth.js |
| `/api/users` | 用户CRUD、修改角色、修改部门 | users.js |
| `/api/departments` | 部门管理（含minister_id + member_id） | departments.js |
| `/api/tasks` | 任务CRUD | tasks.js |
| `/api/files` | 文件提交、审核、退回、重新提交、认证下载 | files.js |
| `/api/summary` | 汇总文件上传下载 | summary.js |
| `/api/notifications` | 通知获取、标记已读 | notifications.js |

### API详细端点

#### 认证 `/api/auth`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/auth/login` | 无（速率限制：5次/15分钟） | 用户登录，设置httpOnly Cookie |
| POST | `/api/auth/logout` | 登录用户 | 退出登录，清除Cookie |
| GET | `/api/auth/me` | 登录用户 | 获取当前用户信息 |
| POST | `/api/auth/change-password` | 登录用户 | 修改密码，递增token_version使旧Token失效 |

#### 用户管理 `/api/users`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/users` | 书记 | 获取用户列表 |
| POST | `/api/users` | 书记 | 创建用户 |
| PUT | `/api/users/:id` | 书记 | 更新用户信息 |
| PUT | `/api/users/:id/role` | 书记 | 修改用户角色 |
| PUT | `/api/users/:id/department` | 书记 | 修改用户部门 |
| DELETE | `/api/users/:id` | 书记 | 删除用户 |

#### 任务管理 `/api/tasks`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/tasks` | 登录用户 | 获取任务列表（支持 `is_regular`、`year` 参数筛选） |
| POST | `/api/tasks` | 书记/副书记/部长 | 创建任务 |
| PUT | `/api/tasks/:id` | 书记/副书记/部长 | 更新任务 |
| DELETE | `/api/tasks/:id` | 书记/副书记 | 删除任务 |

#### 文件管理 `/api/files`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/files` | 登录用户 | 获取文件列表（支持 `task_id`、`status` 参数筛选） |
| POST | `/api/files` | 团支书 | 提交文件（FormData上传） |
| GET | `/api/files/:id/download` | 登录用户 | 认证下载文件 |
| PUT | `/api/files/:id/approve` | 书记/副书记/部长 | 审核通过 |
| PUT | `/api/files/:id/return` | 书记/副书记/部长/副部长 | 退回文件 |
| PUT | `/api/files/:id/resubmit` | 团支书 | 重新提交被退回的文件 |

#### 汇总管理 `/api/summary`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/summary` | 登录用户 | 获取汇总列表 |
| POST | `/api/summary` | 书记/副书记/部长 | 上传汇总文件（FormData上传） |
| GET | `/api/summary/:id/download` | 登录用户 | 认证下载汇总文件 |

#### 通知 `/api/notifications`
| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/notifications` | 登录用户 | 获取通知列表（含未读计数） |
| PUT | `/api/notifications/:id/read` | 登录用户 | 标记已读 |

---

## 前端架构

### 概述
前端采用**单文件SPA架构**，所有CSS、HTML、JavaScript内嵌于 `frontend/index.html` 中。UI设计精美，包含渐变、动画、拖拽上传等交互特性。

### 入口 (frontend/index.html)
- **CSS**：内嵌于 `<style>` 标签，使用CSS变量统一主题，包含渐变背景、卡片动画、响应式布局
- **HTML**：登录页 + 主界面（侧边栏导航 + 内容区） + 8个模态框（任务编辑、任务详情、文件提交、汇总上传、文件打回、用户管理、权限矩阵、部门管理）
- **JavaScript**：内嵌于 `<script>` 标签

### 核心模块

| 模块 | 说明 |
|------|------|
| `API` 对象 | HTTP请求封装（get/post/put/del/upload），统一401跳转和错误处理，`credentials: 'same-origin'` 自动携带Cookie |
| 数据映射函数 | `mapTask`/`mapDept`/`mapUser`/`mapFile`/`mapSummary`/`mapNotif`，将后端snake_case字段映射为前端camelCase格式 |
| `state` 对象 | 全局状态管理（当前用户、导航、年月、筛选条件、待上传文件列表等） |
| 权限检查函数 | `canPublishTask`/`canSubmitFile`/`canReturnFile`/`canUploadSummary`/`canManageUsers`/`canViewAllDepts`/`canAccessDept` |
| UI渲染函数 | `renderContent`/`renderTasks`/`renderFiles`/`renderDepartments`/`renderDeptDetail`/`renderPreviousYear`/`renderPermissions`/`renderNotifications` |
| 卡片组件 | `createTaskCard`/`createFileCard`，带部门色条、状态标签、操作按钮 |
| Toast通知 | `showToast(message, type)`，支持success/error/warning/info四种类型 |
| 拖拽上传 | `setupFileDropZone`/`setupSummaryDropZone`，支持拖拽和点击选择文件 |

### 数据流

```
用户操作 → API调用 → 后端处理 → MySQL存储
    ↓                                      ↓
  UI更新 ← 数据映射 ← API响应 ← 数据库查询
```

### 前端字段映射

| 后端字段 (snake_case) | 前端字段 (camelCase) | 说明 |
|----------------------|---------------------|------|
| department_id | department | 部门ID |
| assigned_to | assignedTo | 指派对象 |
| is_regular | isRegular | 是否常规任务 |
| time_slot | timeSlot | 时间段 |
| created_at | createdAt | 创建时间 |
| minister_id | ministerId | 部长ID |
| member_id | memberId | 成员ID |
| task_id | taskId | 任务ID |
| file_name | fileName | 文件名 |
| file_size | fileSize | 文件大小 |
| submitted_by | submittedBy | 提交者ID |
| submitted_by_name | submittedByName | 提交者姓名 |
| submitted_at | submittedAt | 提交时间 |
| return_reason | returnReason | 退回原因 |
| uploaded_by | uploadedBy | 上传者ID |
| uploaded_at | uploadedAt | 上传时间 |
| target_user | targetUser | 通知目标用户 |
| is_read | read | 是否已读 |

### 初始化流程

```
页面加载 → checkAuth() → GET /api/auth/me
    ↓ 已登录                        ↓ 未登录
  设置currentUser                显示登录页
    ↓
  loadData() → Promise.all([
    GET /api/tasks,
    GET /api/departments,
    GET /api/users,
    GET /api/files,
    GET /api/summary,
    GET /api/notifications
  ]) → 数据映射 → 渲染主界面
    ↓
  启动通知轮询（每30秒）
```

---

## 用户权限与角色

### 角色定义
| 角色值 | 角色名称 | 说明 |
|--------|----------|------|
| secretary | 团委书记 | 最高权限，用户/部门管理 |
| viceSecretary | 副书记 | 任务创建/删除，文件审核 |
| minister | 部长 | 部门任务管理，文件审核 |
| viceMinister | 副部长 | 文件退回 |
| member | 干事 | 部门成员，基础查看 |
| branchSecretary | 团支书 | 文件提交 |

### 权限矩阵

| 功能 | 书记 | 副书记 | 部长 | 副部长 | 团支书 | 干事 |
|------|------|--------|------|--------|--------|------|
| 创建任务 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ |
| 删除任务 | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ |
| 提交文件 | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ |
| 审核文件 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ |
| 打回文件 | ✓ | ✓ | ✓ | ✓ | ✗ | ✗ |
| 重新提交文件 | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ |
| 上传汇总 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ |
| 用户管理 | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| 部门管理 | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| 查看往年 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

---

## 功能模块

### 1. 用户认证
- 登录（httpOnly Cookie，速率限制：5次/15分钟）
- 退出（清除Cookie）
- 修改密码（递增token_version，旧Token立即失效）
- Token有效期：24小时
- 页面加载自动检查登录状态

### 2. 任务管理
- 卡片式任务展示，带部门色条和状态标签
- 常规任务：以年为单位，按频次分组展示（每周/每月/每季度/每学期/每年/不定期）
- 月度任务：按月筛选展示
- 创建/编辑/删除任务
- 任务分类管理（组织生活/学习教育/主题活动/日常工作/其他）
- 任务频次：不定期/每周/每月/每季度/每学期/每年
- 任务完成标记
- 任务冲突检测（同月同部门同时间段）
- 任务详情弹窗（含文件提交和汇总文件列表）
- 搜索和筛选功能

### 3. 文件管理
- 团支书提交文件（拖拽上传 + 点击选择，类型白名单 + 大小限制）
- 负责人审核/退回文件（退回需填写原因）
- 文件重新提交（被退回的文件可重新提交）
- 查看提交状态（待审核/已通过/已打回）
- 认证文件下载（`/api/files/:id/download`）
- 文件卡片展示，带状态色条
- 按部门/状态/文件名筛选

### 4. 汇总管理
- 上传汇总文件（拖拽上传）
- 下载查看汇总
- 认证下载接口

### 5. 部门空间
- 部门卡片展示（带颜色标识、任务数、文件数）
- 部门详情页（任务列表/文件提交/汇总文件/成员信息 四个标签页）
- 部长/部员自动进入本部门详情页
- 部门管理（创建/删除，支持设置部长和部员）

### 6. 往年任务
- 查看去年常态化任务（只读模式）
- 查看历史文件提交和汇总

### 7. 权限管理（仅书记）
- 用户CRUD
- 修改用户角色（下拉选择）
- 修改用户部门（下拉选择）
- 部门管理（创建/删除，支持设置部长和部员）
- 权限矩阵查看

### 8. 通知系统
- 自动生成通知（任务创建、文件提交、审核结果、文件退回、重新提交）
- 未读计数轮询（30秒）
- 标记已读（单个/全部）
- 侧边栏未读徽标

### 9. 审计日志
- 记录关键操作（登录/退出/修改密码/文件提交/审批/打回/上传/删除/重新提交）
- 包含操作者、操作类型、目标、IP地址、时间
- 写入失败不影响业务流程

---

## 安全机制

### 认证与授权

| 机制 | 说明 |
|------|------|
| httpOnly Cookie | Token 存储在 httpOnly Cookie 中，防止 XSS 窃取 |
| SameSite=Lax | 防止 CSRF 攻击 |
| Secure（生产环境） | HTTPS 环境下仅通过加密连接传输 Cookie |
| token_version | 修改密码后旧 Token 立即失效 |
| JWT_SECRET 环境变量 | 密钥不硬编码，未设置时拒绝启动 |

### 请求安全

| 机制 | 说明 |
|------|------|
| Helmet | 自动设置安全响应头（X-Content-Type-Options、X-Frame-Options 等） |
| CORS 域名限制 | 仅允许 `CORS_ORIGIN` 配置的域名访问 API |
| 登录速率限制 | 15 分钟内最多 5 次登录尝试（express-rate-limit） |

### 文件安全

| 机制 | 说明 |
|------|------|
| 类型白名单 | 仅允许 doc/docx/pdf/xls/xlsx/ppt/pptx/txt/csv/jpg/jpeg/png/gif/zip/rar |
| 大小限制 | 默认 10MB，可通过 `MAX_FILE_SIZE` 环境变量配置 |
| 认证下载 | `/uploads` 目录不提供静态服务，文件下载走 `/api/files/:id/download` 认证接口 |
| Nginx deny | Nginx 对 `/uploads` 路径设置 `deny all`，双重保护 |

### 传输安全

| 机制 | 说明 |
|------|------|
| HTTPS | Nginx 80 端口 301 重定向到 443，强制加密传输 |
| TLS 1.2+ | 仅允许 TLSv1.2 和 TLSv1.3 |
| HSTS | `Strict-Transport-Security` 响应头，浏览器强制 HTTPS |

### 数据安全

| 机制 | 说明 |
|------|------|
| 密码加密 | bcrypt 哈希，salt rounds = 10 |
| 生产环境强制配置 | `NODE_ENV=production` 时缺少 `JWT_SECRET`/`DB_PASSWORD` 拒绝启动 |
| .env 不提交 | `.gitignore` 排除 `.env` 文件，防止密钥泄露 |

### 审计追踪

| 机制 | 说明 |
|------|------|
| audit_logs 表 | 记录所有关键操作 |
| IP 地址记录 | 追溯操作来源 |
| 索引优化 | user_id、action、created_at 建立索引，支持高效查询 |

---

## 部署说明

### 前置要求
- **Node.js** 16+
- **MySQL** 8.0+
- **PM2** 5.3.0+（生产环境推荐）
- **Nginx** 1.18+（生产环境推荐）

### 环境变量配置

复制模板并修改：

```bash
cp .env.example .env
```

| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| NODE_ENV | 运行环境 | development | 生产环境必填 |
| DB_HOST | 数据库地址 | localhost | 否 |
| DB_PORT | 数据库端口 | 3306 | 否 |
| DB_USER | 数据库用户 | root | 否 |
| DB_PASSWORD | 数据库密码 | 123456（开发） | **生产环境必填** |
| DB_NAME | 数据库名 | youth_league | 否 |
| PORT | 服务端口 | 3000 | 否 |
| JWT_SECRET | JWT密钥 | 无 | **必须设置** |
| CORS_ORIGIN | 允许的域名 | 无 | 生产环境必填 |
| MAX_FILE_SIZE | 上传大小限制（字节） | 10485760 (10MB) | 否 |

> ⚠️ **生产环境必须配置**：`NODE_ENV=production`、`JWT_SECRET`、`DB_PASSWORD`、`CORS_ORIGIN`  
> 🔑 **生成 JWT 密钥**：`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

### 本地开发

```bash
# 安装依赖
npm install

# 初始化数据库（首次）
npm run init-db

# 启动开发模式（热更新）
npm run dev

# 或普通启动
npm start
```

### 生产部署

```bash
# 一键部署脚本
bash deploy/deploy.sh

# 或手动操作
npm install
cp .env.example .env   # 编辑配置（务必修改 JWT_SECRET 和 DB_PASSWORD）
npm run init-db         # 首次初始化
npm run start:prod      # PM2生产模式启动
```

### 部署检查清单

| 步骤 | 说明 |
|------|------|
| ☐ 安装依赖 | `npm install` |
| ☐ 初始化数据库 | `npm run init-db` |
| ☐ 配置环境变量 | 复制 `.env.example` 为 `.env`，修改 `JWT_SECRET` 和 `DB_PASSWORD` |
| ☐ 生成安全密钥 | `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` |
| ☐ 配置 CORS | 设置 `CORS_ORIGIN` 为实际域名 |
| ☐ 配置 SSL 证书 | 修改 `nginx.conf` 中的证书路径 |
| ☐ 启动服务 | `npm start` 或 `npm run start:prod` |

### npm 命令一览

| 命令 | 说明 |
|------|------|
| `npm start` | 直接启动服务 |
| `npm run dev` | 开发模式（nodemon热更新） |
| `npm run init-db` | 初始化数据库 |
| `npm run start:prod` | PM2生产模式启动 |
| `npm run pm2:start` | PM2启动 |
| `npm run pm2:stop` | PM2停止 |
| `npm run pm2:restart` | PM2重启 |
| `npm run pm2:logs` | 查看PM2日志 |
| `npm run pm2:status` | 查看PM2状态 |

### Nginx 配置

参考 `deploy/nginx.conf`，主要功能：
- **HTTP → HTTPS 重定向**：80 端口 301 到 443
- **SSL/TLS**：仅允许 TLSv1.2+，HSTS 响应头
- **前端静态文件服务**：Gzip 压缩
- **API 反向代理**：proxy_pass 到 Node.js
- **/uploads deny all**：禁止直接访问上传文件
- **安全响应头**：X-Frame-Options、X-Content-Type-Options 等

### 默认账号

所有账号密码均为：`123456`

| 用户名 | 姓名 | 角色 |
|--------|------|------|
| secretary | 张书记 | 团委书记 |
| vice | 李副书记 | 副书记 |
| bgs | 办公室主任 | 部长 |
| zzb | 组织部长 | 部长 |
| xcb | 宣传部长 | 部长 |
| zysjb | 志愿实践部长 | 部长 |
| branch | 陈团支书 | 团支书 |

### 默认部门

| ID | 名称 | 颜色 |
|----|------|------|
| dept1 | 办公室 | #722ed1 |
| dept2 | 组织部 | #13c2c2 |
| dept3 | 宣传部 | #fa8c16 |
| dept4 | 志愿实践部 | #eb2f96 |

---

## 系统架构图

### 本地开发架构

```
┌──────────────────────────────────────┐
│            用户浏览器                  │
│         localhost:3000               │
│  ┌────────────────────────────────┐  │
│  │  单文件SPA (index.html)         │  │
│  │  CSS + HTML + JS 全部内嵌       │  │
│  │  Fetch API + Cookie认证         │  │
│  └────────────────────────────────┘  │
└──────────────┬───────────────────────┘
               │ httpOnly Cookie
               ▼
┌──────────────────────────────────────┐
│       Express 服务器 (3000)           │
│  ┌───────────────────────────────┐  │
│  │  helmet / cors / rate-limit   │  │
│  └───────────────────────────────┘  │
│  ┌───────────┐  ┌────────────────┐  │
│  │ 静态文件   │  │   API 路由      │  │
│  │ /frontend │  │   /api/*       │  │
│  └───────────┘  └───────┬────────┘  │
│                         │            │
│  ┌──────────────────────▼────────┐  │
│  │       MySQL (localhost:3306)   │  │
│  │       数据库: youth_league     │  │
│  └───────────────────────────────┘  │
└──────────────────────────────────────┘
```

### 生产部署架构

```
                    ┌─────────────────┐
                    │     用户浏览器    │
                    │  域名 / 公网IP   │
                    └────────┬────────┘
                             │ HTTPS (HSTS)
                             ▼
                    ┌─────────────────┐
                    │   Nginx (443)    │
                    │   SSL + HSTS     │
                    │   反向代理        │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
     ┌────────────┐  ┌────────────┐  ┌────────────┐
     │  静态文件   │  │  API 请求   │  │  /uploads  │
     │  /frontend │  │  /api/*    │  │  deny all  │
     └────────────┘  └─────┬──────┘  └────────────┘
                           │ proxy_pass
                           ▼
                   ┌─────────────────┐
                   │  Node.js 服务    │
                   │  PM2 进程管理    │
                   │  localhost:3000  │
                   │                 │
                   │  ┌───────────┐  │
                   │  │  helmet   │  │
                   │  │  cors     │  │
                   │  │  rate-limit│ │
                   │  │  Cookie   │  │
                   │  │  审计日志  │  │
                   │  └───────────┘  │
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │     MySQL       │
                   │   数据库服务     │
                   │  (本地/云端)     │
                   └─────────────────┘
```

---

## 更新日志

### v2.4.0 (2026-05-24)
- ✅ 前端架构重构：从模块化IIFE多文件改为单文件SPA，整合GitHub雏形UI
- ✅ UI全面升级：渐变背景、卡片动画、拖拽上传、部门色条、状态标签等精美交互
- ✅ 新增文件重新提交功能：`PUT /api/files/:id/resubmit`
- ✅ 部门创建/更新支持 `member_id` 字段
- ✅ 新增汇总文件认证下载接口：`GET /api/summary/:id/download`
- ✅ 前端数据映射层：统一后端snake_case与前端camelCase字段转换
- ✅ 前端初始化流程：页面加载自动检查登录状态，未登录显示登录页
- ✅ 文件上传改为FormData，不再使用Base64编码
- ✅ 移除旧模块化前端文件（js/api、js/utils、js/views、js/app.js、css/style.css）

### v2.3.0 (2026-05-23)
- ✅ 修复左上角导航栏标题：从"工作程序"改为"办公系统"
- ✅ 修复常规任务显示问题：常规任务查询时不按用户部门过滤，所有用户可见
- ✅ 新建任务时`department_id`允许为空
- ✅ 后端部门负责人通知逻辑增加非空判断

### v2.2.0 (2026-05-23)
- ✅ 部门空间只显示部长/负责人（包括副部长），不再显示部员
- ✅ 修复新建任务500错误：category 改为下拉选择（匹配数据库ENUM），assigned_to 改为下拉选择，提交字段名统一为下划线格式
- ✅ 后端 GET /api/departments 返回该部门所有部长/副部长列表
- ✅ 部门详情页"成员信息"只显示部长/副部长

### v2.1.0 (2026-05-22)
- ✅ 系统名称更改为「计算机学院团委办公系统」
- ✅ 修复部门映射：helpers.js 中部门ID从数字key改为字符串key（dept1/dept2/dept3/dept4），修复修改部门后显示"未设置"的问题
- ✅ 修复新建任务400错误：频次选项从英文值改为中文值（匹配数据库ENUM），字段名统一为下划线格式，department_id 不再 parseInt
- ✅ 常规任务以年为单位按频次分组展示，月度任务按月展示
- ✅ 后端 GET /api/tasks 新增 `is_regular` 查询参数支持

### v2.0.0 (2026-05-22) — 安全加固版

**P0 严重安全修复：**
- 🔒 JWT 密钥改用环境变量，未设置时拒绝启动
- 🔒 移除 `/uploads` 静态文件服务，文件下载走认证接口
- 🔒 文件上传添加类型白名单和大小限制

**P1 高危安全修复：**
- 🔒 CORS 限制为配置域名
- 🔒 登录接口添加速率限制（5次/15分钟）
- 🔒 生产环境强制要求环境变量
- 🔒 Nginx HTTP 重定向到 HTTPS + HSTS

**P2 中等安全修复：**
- 🔒 Token 存储从 localStorage 改为 httpOnly Cookie
- 🔒 新增 token_version 机制，修改密码后旧 Token 立即失效

**P3 安全增强：**
- 🔒 添加 Helmet 安全响应头中间件
- 🔒 新增审计日志系统（audit_logs 表）

**其他适配：**
- 新增依赖：helmet、express-rate-limit、cookie-parser
- .env.example 新增 NODE_ENV、CORS_ORIGIN 配置项
- 前端 API 客户端改用 Cookie 认证

### v1.1.0 (2026-05-22)
- ✅ 新增云端部署支持（PM2、Nginx、一键部署脚本）
- ✅ 新增环境变量配置（.env.example）
- ✅ 新增 PM2 进程管理配置（ecosystem.config.js）
- ✅ 新增 Nginx 反向代理配置
- ✅ 新增一键部署脚本（deploy/deploy.sh）
- ✅ 更新 .gitignore（忽略敏感文件和上传目录）
- ✅ 更新 package.json（新增 PM2 和开发命令）

### v1.0.0 (2026-05-22)
- ✅ 修复用户管理API路径错误（/auth/users → /users）
- ✅ 新增 `viceMinister`（副部长）角色
- ✅ 完善用户管理功能（修改角色、部门接口）
- ✅ 统一部门ID格式（字符串UUID：dept1/dept2...）
- ✅ 修复前端部门选择器ID不匹配问题
