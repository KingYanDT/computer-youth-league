# 更新日志

## 2026-07-05 前端单文件拆分为三文件

### 变更内容
将 `frontend/index.html` 单文件前端应用拆分为三个独立文件，提升可维护性：

- `frontend/styles.css`（234 行）：提取全部 CSS 内容（原 index.html 第 8-241 行）
- `frontend/app.js`（2128 行）：提取全部 JavaScript 内容（原 index.html 第 560-2687 行）
- `frontend/index.html`（326 行）：仅保留 HTML 结构 + 两个外部引用标签

### 拆分细节
- 原 `<style>...</style>` 块替换为 `<link rel="stylesheet" href="styles.css">`
- 原 `<script>...</script>` 块替换为 `<script src="app.js"></script>`（无需 defer，位于 body 末尾，DOM 已加载）
- CSS 和 JS 内容原样提取，未修改任何字符
- HTML 结构（body 内容）保持不变

### 验证结果
- Grep 确认 index.html 中无残留 `<style>` 标签
- Grep 确认 index.html 中仅有一个 `<script src="app.js"></script>` 外部引用
- 三个文件行数：styles.css = 234 行，app.js = 2128 行，index.html = 326 行

### 下一步工作
- 在浏览器中测试拆分后的页面，确认样式与功能正常
- 如需要，可进一步对 app.js 按功能模块拆分（如 api.js、render.js、auth.js 等）
- 考虑为 CSS 和 JS 添加版本号或哈希指纹以优化缓存策略

## 2026-07-06 提交并上传 GitHub（master）

### 变更内容
将本轮全部改动提交至 GitHub（origin/master），主要包含：

- 前端：index.html 单文件拆分为 index.html / app.js / styles.css 三文件（详见 2026-07-05 条目）
- 后端：新增 backend/utils/db.js、pagination.js、upload.js 公共工具模块；重构各路由与文件上传逻辑
- 文档：重写 SPEC.md，更新 .gitignore（排除 github-prototype/、mysql/ 等目录）
- 部署：更新 deploy/deploy.sh、ecosystem.config.js 与 launcher 构建脚本
- 清理：移除根目录旧 index.html 与 系统架构.md（架构说明统一并入 SPEC.md）
- 新增 更新日志.md（本文件）作为项目变更记录

### 提交说明
- 排除仓库内误命名的重复文件 更新日志.md.md（实为旧系统架构文档，已被 SPEC.md 取代）
- 提交前已确认 .env、node_modules/、mysql/、logs/、uploads/ 等敏感/体积目录被 .gitignore 忽略

### 下一步工作
- 在浏览器中验证前端三文件拆分后的页面功能与样式
- 对 app.js 按功能模块进一步拆分（api.js / render.js / auth.js 等）
- 为静态资源（CSS/JS）添加版本号或哈希指纹以优化缓存策略
- 视需要清理本地遗留的 更新日志.md.md 误命名文件（重命名为 系统架构-旧.md 或直接删除）
